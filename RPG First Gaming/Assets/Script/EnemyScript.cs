using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyScript : Entity
{
    // Start is called before the first frame update
    void Start()
    {
        InitializedEntity(100, 50);   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
